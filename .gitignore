node_modules/
.DS_Store
*.log

# ⛔ RECOVERED SOURCE DOCUMENTS NEVER ENTER THE REPO.
# Public-domain status makes a TM publishable-in-principle; it does not make it
# our business to redistribute the whole book. We publish the PLATE, verified
# and provenanced, not the document it came out of.
.plate-cache/
